import React from "react";
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SingleUpdateScreen = () => {
  return (
    <SafeAreaView>
      <Text>SingleUpdateScreen</Text>
    </SafeAreaView>
  );
};

export default SingleUpdateScreen;
